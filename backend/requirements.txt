# ── Web framework & ASGI server ──────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── Validation & settings ────────────────────────────────────────────────────
pydantic[email]
pydantic-settings

# ── Database: async Postgres + pgvector + migrations ─────────────────────────
sqlalchemy[asyncio]
asyncpg
pgvector
alembic

# ── Auth ─────────────────────────────────────────────────────────────────────
python-jose[cryptography]
bcrypt==4.0.1

# ── LLM / agent stack ────────────────────────────────────────────────────────
anthropic
langchain-anthropic
langchain-core
langgraph

# ── RAG embeddings (torch is installed CPU-only in the Dockerfile) ───────────
sentence-transformers

# ── HTTP client & utilities ──────────────────────────────────────────────────
httpx
structlog
tenacity
cachetools
